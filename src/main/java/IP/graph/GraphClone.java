package main.java.IP.graph;

import java.util.*;

public class GraphClone {

   /* private static Map<Node, ArrayList<Node>> adjList = new HashMap<>();

    private static HashSet<Node> visited = new HashSet<Node>();

    public static Node build_other_graph(Node node) {

        if (node == null) return null;

        bfs(node);


    }

    private static void bfs(Node node) {
        visited.add(node);
        Node newNode = new Node(node.val);
        adjList.put(node, newNode);
        Queue<Node> queue = new LinkedList<>();
        queue.add(node);

        while (!queue.isEmpty()) {
           Node node1 =  queue.poll();

        }


    }


    static class Node {
        Integer val;
        List<Node> neighbours = new ArrayList<>();

        Node(Integer _val) {
            val = _val;
            neighbours.clear();
        }
    }*/




}
